[pytest]
addopts =
    --color=yes
    --tb=auto
    --doctest-glob='*.rst'
    --doctest-modules -vv
    --ignore setup.py
norecursedirs =
    src
    dist
    build
    wheelhouse
    ignore
    .git
    .tox
